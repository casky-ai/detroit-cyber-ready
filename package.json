{
  "name": "detroit-cyber-ready",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "test": "pnpm -r --if-present run test",
    "db:seed": "dotenv -e .env.local -- tsx scripts/seed-detroit-data.ts",
    "map:build": "tsx scripts/build-detroit-basemap.ts"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "@dcr/impact": "workspace:*",
    "@supabase/supabase-js": "^2.49.0"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.19.0"
  }
}
